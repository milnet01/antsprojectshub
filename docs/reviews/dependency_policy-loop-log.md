# Review loop log — docs/DEPENDENCY_POLICY.md

Rows for [`docs/DEPENDENCY_POLICY.md`](../DEPENDENCY_POLICY.md), one per `review-contract`
loop, oldest first.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Verified / fixed / dismissed | Outcome |
|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-26 | 2, every lane held every question; both arrived holding the three CLAUDE.md files, the memory index and commit subjects f6ea7ea and 04035d2 | 1 | 1 | 2 | — | 4 / 4 / 0 | Gate armed by f6ea7ea (whole-file rewrite). Q1: the re-pin command read `git/ref/tags`, which gives a tag object's SHA for an annotated tag — now `commits/<tag>`; all five actions' tags are lightweight today, so latent. Q2: "current LTS" here against "latest stable" in CLAUDE.md, with Node Current v26 and LTS v24 — the file now states LTS as this repo's reading and CLAUDE.md § Dependencies says so; the check command now prints the pinned major. Q3 (both from lane questions settled by one check each): CLAUDE.md's `Node >= 20` quote and the `runs-on: ubuntu-latest` runner image added to where versions live. Two open questions resolved clean: PySide6 has no version string anywhere, and `check-dependencies` reads the ledger by judgement, so the `_example_` row is not parsed as a hold. |
| 2 | 2026-09-26 | 2, every lane held every question; both arrived holding the three CLAUDE.md files, the memory index and commit subjects b991e52 and f6ea7ea (so each knew a loop 1 ran, not what it found) | 0 | 0 | 1 | — | 1 / 1 / 3 | Q3: the Node bullet put the LTS-tracking pin and the `engines.node` floor under one "Node tracks the newest LTS" sentence, so a conformer could raise the floor with the pin — split into a pin bullet and a floor bullet (standard §4). The finding landed on text loop 1 wrote. Dismissed: a Q2 quoting CLAUDE.md § Dependencies without the "tracks the newest LTS" clause loop 1 added (quotation not in current text); a "last swept" date line (a state snapshot, which standard §8 keeps out of a policy file); the `_example_` ledger row (resolved clean in loop 1). PySide6 raised again as an open question and again resolved clean. |
| 3 | 2026-09-26 | 2, every lane held every question; both arrived holding the three CLAUDE.md files, the memory index and commit subjects b991e52 and d79543c (so each knew two loops ran, not what they found); one lane's pre-loaded CLAUDE.md predated loop 1's edit | 1 | 1 | 2 | — | 4 / 3 / 0, 1 surfaced | Cap (3, standard). Q1: `local-CI.sh:9` said "Node 24 check", a second copy of `CI_NODE_MAJOR` — comment now names no version. Q3: the Node floor was unchecked, and a lane's open question settled by one command found it FALSE: `sanitize-html` 2.17.7 declares `engines.node >=22.12.0` against our `>=20` — `package.json`, the lockfile root and CLAUDE.md's quote now say 22.12, and the file gives the command that finds the floor. Q3 (both lanes; raised in loop 2 too and dismissed there on standard §8 — reversed: a sweep date is not a version snapshot): the file now says a sweep that finds nothing leaves no record, so the cadence is unchecked. SURFACED, not fixed: Q2 — the file's LTS reading of "latest stable" for Node conflicts with standard §1, which allows holding only where a newer version breaks something; Node Current is v26, CI pins 24. Whether to keep an LTS override (then in `docs/standards/dependencies-overrides.md`) or follow §1 and move to 26 is the user's decision. The file's own list of what it holds now names the Node reading, closing the internal half. Own-fix share unreadable (f6ea7ea rewrote the whole file); on substance two of four findings repaired loop-1 and loop-2 text, so this cap is not calm. |
